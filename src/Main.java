public class Main {
    public static void main(String[] args) {

        // Create a new instance of the generateUI class
        generateUI uiStart = new generateUI();
        uiStart.createUI();
    }
}